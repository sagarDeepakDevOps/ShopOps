# Jaeger

Jaeger runs as `jaegertracing/all-in-one` in development via root `docker-compose.yml`.

For production, replace this with a distributed Jaeger deployment (collector, query, agent) or an OTLP-compatible tracing backend.
